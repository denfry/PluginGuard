# Java / Gradle
analyzer/.gradle/
analyzer/build/
analyzer/sandbox-runtime/.gradle/
analyzer/sandbox-runtime/build/
*.class

# Plugin samples dropped at the repo root for manual testing (may be third-party
# binaries we have no right to redistribute — never commit them)
/*.jar

# Node / Next.js
web/node_modules/
web/.next/
web/out/
web/next-env.d.ts

# Built sample plugins (generated by sample-plugins/build.*)
sample-plugins/*.jar

# Tooling / scratch
.idea/
.vscode/
*.log
.playwright-mcp/
web/.playwright-mcp/
pg-*.png
.DS_Store

# Local Claude config (personal hooks / settings)
.claude/settings.local.json
